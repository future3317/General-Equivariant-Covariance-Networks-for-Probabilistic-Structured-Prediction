## 总体判断

你们的新模型显著改善了数值条件和部分联合校准，但没有解决核心统计问题：

* Mahalanobis² 从 33.96 降到 19.87，下降约 41.5%，但仍约为理论值 10 的 1.99 倍。
* coverage@90 从 70.8% 升到 75.8%，仍有明显过度自信。
* condition number 从 555 降到 20.9，下降约 96.2%，说明 spectral window 确实抑制了病态谱。
* physical MAE 从 2.075 恶化到 2.409，约增加 16.1%，说明 covariance regularization、共享特征和 mean optimization 之间存在明显耦合。

“marginal standard deviation 基本合理、相关矩阵不一致”与 covariance orientation mismatch 一致，但尚不能据此确诊。当前最值得优先排查的是：

1. Student-t scale matrix 与 covariance matrix 的约定是否混用。
2. 逐样本条件 whitening 后是否仍存在角向各向异性。
3. covariance head 是否真正包含 (2\times 0e)、(2\times2e)、(4e) 的全部独立路径。
4. mean/covariance 联合 NLL 是否使 covariance 分支损害了 point predictor。
5. 固定 spectral window 是否把“总体尺度”和“条件数”不必要地绑在了一起。

---

## 1. 首先审计 Student-t 的参数约定

设输出维数 (d=6)，残差 (r=y-\mu(x))，Student-t 的 scale matrix 为 (S(x))：

[
\delta=r^\top S^{-1}r.
]

对于标准椭圆型多元 Student-t，

[
\delta\sim dF_{d,\nu},\qquad
\mathbb E[\delta]=\frac{d\nu}{\nu-2}.
]

你们给出的理论期望约为 10，结合 (d=6)，对应 (\nu=5)：

[
\frac{6\cdot5}{5-2}=10.
]

但 Student-t 的实际 covariance 是

[
C=\frac{\nu}{\nu-2}S.
]

若 Mahalanobis² 使用的是 (C^{-1})，理论期望应为

[
\mathbb E[r^\top C^{-1}r]=d=6,
]

而不是 10。90% 椭球覆盖阈值也必须使用

[
\delta_{0.9}=dF^{-1}_{d,\nu}(0.9),
]

不能使用 (\chi_d^2) 阈值。Pyro 的 `MultivariateStudentT` 明确以 `scale_tril` 参数化，而不是 covariance；PyTorch 核心目前提供多元正态和一元 `StudentT`，因此自定义实现尤其容易出现这个约定错误。([Pyro 文档][1])

建议在代码中始终区分：

```text
student_scale
student_covariance = nu / (nu - 2) * student_scale
```

并分别测试：

[
\mathbb E[r^\top S^{-1}r]\approx10,\qquad
\mathbb E[r^\top C^{-1}r]\approx6.
]

这是第一优先级，因为约定混用会同时污染 Mahalanobis、coverage、marginal standard deviation 和 calibration plots。

---

## 2. 不要直接用全局 correlation matrix 诊断条件 orientation

全局 residual covariance 满足

[
\mathbb E[rr^\top]
==================

\mathbb E[\operatorname{Cov}(Y\mid X)]
+
\mathbb E[b(X)b(X)^\top],
]

其中

[
b(X)=\mathbb E[Y\mid X]-\mu(X).
]

因此它同时包含条件噪声、mean bias 和不同输入分布的混合。另一方面，

[
\operatorname{Corr}\big(\mathbb E[S(X)]\big),
\quad
\mathbb E[\operatorname{Corr}(S(X))]
]

也不是同一个量。全局 predicted correlation 与 residual correlation 不一致，可能来自：

* 条件 eigenspace 错位；
* mean bias；
* heteroscedastic mixture；
* Student-t 之外的非椭圆残差；
* 少量重尾样本；
* 对 correlation matrix 进行非线性平均的方式。

更可靠的诊断是逐样本对称 whitening：

[
z_i=S_i^{-1/2}r_i.
]

若 (S_i) 是 (\nu=5) Student-t scale，则应满足

[
\mathbb E[z_iz_i^\top]=\frac{5}{3}I_6,
\qquad
\mathbb E[|z_i|^2]=10.
]

建议报告三个互补量。

第一，径向校准：

[
u_i=F_{d,\nu}\left(\frac{|z_i|^2}{d}\right),
]

理想情况下 (u_i\sim\operatorname{Uniform}(0,1))。用完整 PIT/coverage curve，而不只是 coverage@90。

第二，角向校准：

[
M_z=\frac1n\sum_i z_iz_i^\top,
\qquad
A_{\rm white}
=============

\left|
\frac{\nu-2}{\nu}M_z-I
\right|_F.
]

若 (A_{\rm white}) 大、但径向 PIT 经一个 scalar temperature 后正常，才是较明确的 orientation/shape mismatch。

第三，群表示分解。对

[
D_i=z_iz_i^\top-\frac{\nu}{\nu-2}I
]

做 CG 投影。对当前表示，

[
\operatorname{Sym}^2(0e\oplus2e)
================================

2\cdot0e\oplus2\cdot2e\oplus4e.
]

理想模型下，各通道的条件均值都应为零。尤其应区分：

* 与 (I_6) 正交的另一个 (0e)：trace 与 deviatoric 子空间之间的相对尺度错误；
* 两个 (2e)：scalar–deviatoric coupling 和 deviatoric covariance shape；
* (4e)：(2e) 子空间内部更高阶的各向异性，通常与 orientation mismatch 最直接相关。

这个 irrep-resolved whitening defect 可以成为论文中比普通 correlation heatmap 更有价值的 symmetry-aware calibration diagnostic。

---

## 3. 五类问题必须严格分开

| 层次                      | 回答的问题                                                       | 你们当前状态                         |
| ----------------------- | ----------------------------------------------------------- | ------------------------------ |
| 理论 equivariance         | 旋转/反射后，分布是否按 push-forward 正确变换                              | SPD 与数值 equivariance 测试通过，基本解决 |
| Covariance expressivity | 网络能否表示真实的条件 covariance 函数                                   | 尚未证明；hidden width 不是充分条件       |
| Statistical calibration | 概率、形状、尾部和覆盖是否与残差分布一致                                        | 明显未解决                          |
| Numerical conditioning  | SPD 运算、梯度、条件数是否稳定                                           | 新 window 显著改善                  |
| Engineering correctness | scale/covariance、CG normalization、basis、dtype、quantile 是否一致 | 必须重新审计                         |

近期关于 equivariant uncertainty calibration 的理论工作指出，错误的 symmetry assumption 会系统性损害 calibration，但正确 equivariance 本身并不保证 calibration；概率等变轨迹模型、等变惯性里程计以及近期全 SPD 等变 uncertainty 模型也都表明，几何一致性和统计可靠性是两个独立目标。([arXiv][2])

---

## 4. Covariance head 是否真的“full”

对

[
V=0e\oplus2e
]

而言，完整 symmetric covariance 空间是 21 维：

[
\operatorname{Sym}^2(V)
=======================

2\cdot0e\oplus2\cdot2e\oplus4e,
]

维数为

[
2(1)+2(5)+9=21.
]

因此，一个声称 full covariance 的 head 至少需要满足：

1. 两个 (0e) copy 没有被错误合并。
2. 两个 (2e) copy 有独立 multiplicity mixing。
3. 存在完整 (4e) 路径。
4. 上游 hidden irreps 能够生成这些路径。
5. CG lifting 后的输出 Jacobian 在典型样本附近具有接近 21 的有效秩。

增加 hidden width 只能增加潜在 capacity；若 tensor-product path、multiplicity basis 或 (l_{\max}) 构造有瓶颈，实际 covariance orientation 仍可能处在低维流形上。e3nn 的 `FullyConnectedTensorProduct` 会为所有兼容 irreps 创建路径并学习其加权组合，但仍需要显式检查 path multiplicity、输出布局和 reduced tensor-product normalization。([E3NN][3])

建议做以下结构消融：

[
2\cdot0e,\quad
2\cdot0e\oplus2\cdot2e,\quad
2\cdot0e\oplus2\cdot2e\oplus4e.
]

若加入 (4e) 后 whitened angular defect 显著下降，基本可以确认之前的 orientation capacity 不足。还应对 covariance coefficients 关于 frozen backbone features 的 Jacobian 做 SVD，而不是只比较参数量。

---

## 5. 为什么 Student-t 仍会 scale collapse

Student-t 并不会自动消除 heteroscedastic scale collapse。对各向同性 scale (S=s^2I)，单样本 NLL 中与 (s) 有关的部分是

[
\ell(s)
=======

d\log s+
\frac{\nu+d}{2}
\log\left(
1+\frac{|r|^2}{\nu s^2}
\right).
]

对 (\log s) 求导并令其为零，可得

[
s^2=\frac{|r|^2}{d}.
]

因此当训练残差趋近零时，最优 scale 同样趋近零。对于完全自由的 full covariance，残差正交方向的 eigenvalue 甚至可以趋近零而不增加 Mahalanobis 项，使 log-determinant 不受限制地下降。实际网络中的样本共享、window 和正则化只是阻止这种退化，而不是消除其优化倾向。

Student-t 的 location gradient 为

[
\nabla_\mu\ell
==============

-\frac{\nu+d}{\nu+\delta}S^{-1}r.
]

这带来两个问题：

* 大残差被 Student-t 权重进一步降权；
* 错误的 covariance orientation 会在 (S^{-1}) 中错误地旋转 mean gradient。

因此共享 backbone 时，covariance head 可以通过“解释”难样本来改变 point predictor。heteroscedastic Gaussian NLL 已有系统研究表明，mean 与 variance 的联合梯度可能产生不稳定或较差的均值解；Faithful Heteroscedastic Regression 的核心做法是阻断 covariance 分支对共享 mean representation 的影响，并让 mean 保持 mean-only baseline 的更新行为。该论文直接针对 Gaussian，Student-t 版本需要重新推导或采用显式分阶段训练，不能无条件宣称同样的理论保证。([Proceedings of Machine Learning Research][4])

对你们当前结果，最有价值的实验不是继续同时增加 width，而是：

1. 取 physical MAE 最好的 mean checkpoint。
2. 完全冻结 mean head。
3. covariance head 使用 detached backbone，或使用独立的小型 equivariant adapter。
4. 用 out-of-fold/cross-fitted residual 训练 covariance。
5. 只在最后进行很小学习率的联合 fine-tuning。

如果这能恢复 2.075 左右的 MAE，同时保留新模型的条件数和 calibration 改善，就能确认主要是 mean–covariance gradient interference。

---

## 6. SPD 参数化的比较

### Matrix exponential

[
S=\exp(H),\qquad H=H^\top.
]

优点是从 (\operatorname{Sym}(V)) 到 (\operatorname{SPD}(V)) 全表达、无 Cholesky basis ordering，并且

[
H(gx)=\rho(g)H(x)\rho(g)^\top
\Rightarrow
\exp H(gx)
==========

\rho(g)\exp H(x)\rho(g)^\top.
]

缺点是无界 log-spectrum 会造成极大的条件数、logdet 梯度和数值误差。对 6×6 矩阵，计算成本通常不是主要问题，谱宽才是问题。

### Spectral window

若 window 作用在 (H=\log S) 的 eigenvalues 上，则

[
\lambda(H)\in[-4,4]
\Rightarrow
\kappa(S)\le e^8\approx2981,
]

而

[
\lambda(H)\in[-2,2]
\Rightarrow
\kappa(S)\le e^4\approx54.6.
]

这与 condition number 从 555 降至 20.9 完全一致。但固定 ([-2,2]) 同时限制了总体 scale 和 anisotropy；若真实条件 covariance 的 log-volume 或 anisotropy 超出该范围，会造成结构性欠拟合。使用 `tanh` window 还会在边界产生梯度饱和。

更好的参数化是将 log-volume 与 centered log-shape 分开：

[
H=s(x)I+\bar H(x),
\qquad
\operatorname{tr}\bar H=0,
]

[
\bar H
======

w,f_{\rm spec}(A),
]

其中 centered spectrum 被限制在 ([-w,w])，而 (s(x)) 使用独立、较宽的 bounds 或 prior。这样

[
\kappa(S)\le e^{2w},
]

但整体 uncertainty level 不必被同一个 window 限死。为防止 scale collapse，可以只对 (s(x)) 设置较宽的下界或 log-scale regularizer。

### Cholesky / log-Cholesky

Cholesky 参数化保证 SPD，triangular solve 和 logdet 高效，也已有多元 covariance regression 工作使用这一路径。其根本问题是 raw lower-triangular head 依赖固定坐标顺序：

[
L(gx)\neq\rho(g)L(x)
]

通常也不能推出

[
L(gx)L(gx)^\top
===============

\rho(g)LL^\top\rho(g)^\top.
]

因此它不适合作为直接 O(3)-equivariant covariance head。安全做法是先产生等变 SPD (S)，再调用 `torch.linalg.cholesky(S)` 计算 NLL；Cholesky 只是数值分解，不承担 representation semantics。

### (BB^\top+\epsilon I)

若 (B(x)) 具有正确的 equivariant operator transformation，这种参数化适合 low-rank-plus-isotropic floor。缺点是 factor gauge 不唯一，且小 rank 限制 orientation expressivity。

### Precision head

直接预测

[
\Lambda=S^{-1}=\exp(H_\Lambda)
]

与 covariance head 在理论表达能力上等价，有时 NLL 中的二次项更直接。但它不会自动解决 calibration；window、scale collapse 和 orientation capacity 仍然存在。

Geoopt 和 Geomstats 提供 SPD manifold、切空间、log/exp 和相关 Riemannian 几何工具，适合全局 calibration matrix、geodesic regularization 和分析；对于每个样本都输出 SPD 的深度网络，通常无需把全部网络参数放到 manifold optimizer 上。([Geoopt][5])

---

## 7. Low-rank、precision graph 与 residual modeling

固定 Kelvin 坐标下的 “diagonal + low rank”

[
D+UU^\top
]

通常不保持连续 O(3) equivariance，因为“对角”不是旋转不变量。对应的等变版本应写成

[
S(x)
====

\sum_{\ell}\sigma_\ell^2(x)P_\ell
+
\sum_{a=1}^{r}u_a(x)u_a(x)^\top,
]

其中：

* (P_\ell) 是 (V) 各 irreducible block 的正交 projector；
* (\sigma_\ell(x)) 是 invariant scalar；
* (u_a(x)\in V) 是 equivariant vectors。

对 (V=0e\oplus2e)，baseline 可以是

[
\sigma_0^2P_{0e}+\sigma_2^2P_{2e}.
]

它是普适的 equivariant low-rank-plus-irrep-isotropic 结构。小 rank 可改善条件数和 sample efficiency，但会限制 eigenspace，应该作为 capacity–calibration trade-off baseline，而不是默认替代 full covariance。

固定 graph sparsity 施加在六个 Kelvin components 的 precision matrix 上一般会破坏 O(3)。对 multiplicity-free 的 (0e\oplus2e)，任何与全部 (\rho(g)) 对易的固定线性 operator，按 Schur 结构只能是

[
aP_{0e}+bP_{2e}.
]

因此任意 component graph 不是物理不变量。Graph precision 只有在图节点本身是物理对象或 irreps，而不是坐标分量时才合理。

残差外积

[
r_ir_i^\top
]

是 rank-1、方差很高的 covariance pseudo-label。建议将它用于：

* cross-fitted conditional moment regression；
* batch-level whitening moment loss；
* shrinkage diagnostics；

而不是逐样本强制拟合。近期工作也在研究利用 residual-derived covariance pseudo-label 和 Wasserstein 类型目标辅助深度 heteroscedastic covariance estimation。([ICLR Proceedings][6])

---

## 8. 应选哪种 calibration loss

### 主目标：Student-t NLL

NLL 是最自然的主损失，因为它同时识别 location、volume、shape、orientation 和 tail parameter。严格 proper scoring rule 的理论说明，真实预测分布在期望意义下优化 proper score；但“proper”不意味着有限样本优化容易，也不意味着一个受限神经参数族能恢复真实条件分布。([华盛顿大学统计网站][7])

建议固定或弱正则化 (\nu)。若同时让网络自由预测 (\nu(x))、scale 和 orientation，可识别性会进一步变差。

### Energy score

Energy score 是坐标正交变换不变的，适合作为与 O(3) 兼容的辅助 proper score：

[
ES(P,y)
=======

\mathbb E|X-y|
-\frac12\mathbb E|X-X'|.
]

但关于它对 correlation/dependence 错误的有限样本敏感度，文献结论并不完全一致；一些研究发现其区分相关结构的能力较弱，另一些研究在不同实验设计下得到更积极结论。不能仅依赖一个 aggregate energy score 判断 orientation。([arXiv][8])

### Variogram score

Variogram score 经常比 energy score 更敏感于 component dependence，但标准形式基于固定坐标分量差：

[
|Y_i-Y_j|^p,
]

因此在 Kelvin basis 中通常不是 O(3)-invariant。直接把它加入训练会引入 preferred coordinate frame。原始 variogram score 是 proper 但通常不是 strictly proper。([气象学期刊][9])

要使用它，需采用群平均：

[
VS_G(P,y)
=========

\mathbb E_{g\sim O(3)}
VS(\rho(g)_#P,\rho(g)y),
]

或构造基于 invariant/equivariant contractions 的版本。

### 推荐的 orientation-sensitive proper auxiliary：isotropic sliced CRPS

定义

[
S_{\rm slice}(P,y)
==================

\mathbb E_{a\sim\operatorname{Unif}(S^5)}
\operatorname{CRPS}
\left(
P_{a^\top Y},
a^\top y
\right).
]

对多元 Student-t，任意投影 (a^\top Y) 仍是一元 Student-t，scale 为

[
\sqrt{a^\top Sa}.
]

对球面方向均匀积分后，该 score 对任意正交 basis change 不变，同时直接检测 covariance orientation，因为 orientation 决定所有投影方差 (a^\top Sa)。这比固定坐标 variogram 更符合你们的群结构。Conditional CRPS 等多元 scoring-rule 研究也显示，针对条件分布或投影结构设计的分数可以提高对 correlation 的敏感度。([arXiv][10])

### Coverage loss

不建议作为主训练损失。单个 nominal level 的 coverage loss 可以通过无限膨胀 covariance 或错误形状达到目标，不奖励 sharpness，也不是 proper density score。只用于：

* validation；
* 后处理温度选择；
* 多 level calibration curve；
* 最终 conformal threshold。

### Conformal covariance calibration

使用

[
q_i=r_i^\top S_i^{-1}r_i
]

作为 nonconformity score，可构造椭球预测集：

[
\mathcal C_\alpha(x)
====================

\left{
y:
(y-\mu)^\top S^{-1}(y-\mu)\le\hat q_{1-\alpha}
\right}.
]

在 exchangeability 条件下，它提供 finite-sample marginal coverage；已有多元 conformal 工作采用 Mahalanobis/local covariance 构造椭球，并进一步研究 heteroscedastic conditional shape。([arXiv][11])

但 conformal threshold 主要是一个 scalar radial correction：

* 它不能修复错误 eigenspace；
* shape 错时仍可能得到 90% coverage，但椭球体积很大、方向低效；
* 它不使 Student-t density 本身 calibrated。

因此正确顺序是：

[
\text{orientation calibration}
\rightarrow
\text{spectral/radial calibration}
\rightarrow
\text{split conformal coverage}.
]

学习 orientation 的数据和计算 conformal quantile 的数据必须分开，否则标准 split-conformal guarantee 不再直接成立。

---

# 9. 推荐的 TPAMI 方法：Equivariant Isospectral Orientation Calibration

建议把方法设计成一个普适模块，暂称 **EIOC：Equivariant Isospectral Orientation Calibration**。

## 9.1 基础 covariance

基础网络产生 Student-t scale：

[
S_\theta(x)\in\operatorname{SPD}(V),
]

满足

[
S_\theta(gx)
============

\rho(g)S_\theta(x)\rho(g)^\top.
]

## 9.2 用 skew-symmetric Lie algebra 预测 orientation correction

预测

[
K_\phi(x)\in\mathfrak{so}(V)
\cong\Lambda^2(V),
\qquad
K_\phi^\top=-K_\phi,
]

并要求

[
K_\phi(gx)
==========

\rho(g)K_\phi(x)\rho(g)^\top.
]

定义

[
Q_\phi(x)=\exp K_\phi(x)\in SO(6),
]

[
S_{\rm ori}(x)
==============

Q_\phi(x)S_\theta(x)Q_\phi(x)^\top.
]

对 dielectric 表示，

[
\Lambda^2(0e\oplus2e)
=====================

1e\oplus2e\oplus3e,
]

总维数

[
3+5+7=15,
]

恰好对应 6D covariance eigenspace orientation 的 15 个连续自由度。

### 等变性证明

由于 matrix exponential 与 conjugation 相容，

[
Q_\phi(gx)
==========

\rho(g)Q_\phi(x)\rho(g)^\top.
]

于是

[
\begin{aligned}
S_{\rm ori}(gx)
&=
Q_\phi(gx)S_\theta(gx)Q_\phi(gx)^\top\
&=
\rho(g)Q_\phi(x)S_\theta(x)Q_\phi(x)^\top\rho(g)^\top\
&=
\rho(g)S_{\rm ori}(x)\rho(g)^\top.
\end{aligned}
]

因此该校准层严格保持 (O(3)) equivariance。

### 关键性质

它保持：

[
\operatorname{eig}(S_{\rm ori})
===============================

\operatorname{eig}(S_\theta),
]

[
\det S_{\rm ori}=\det S_\theta,
\qquad
\kappa(S_{\rm ori})=\kappa(S_\theta).
]

所以训练 (K_\phi) 时，Student-t NLL 的 logdet 项完全不变，优化只通过 Mahalanobis 项调整 orientation。这使“orientation 是否是主要问题”成为可证伪的实验：

* 若 K-only 显著改善 NLL、Mahalanobis 和 angular whitening，而 eigenvalues、logdet、condition number 完全不变，则 orientation hypothesis 得到直接支持。
* 若改善很小，则问题主要是 spectrum、mean bias 或 distribution-family mismatch。

直接校准 eigenvectors 会遇到排序、符号和重复 eigenvalue 下的不连续；skew exponential 不显式选择 eigenvectors。在谱退化子空间中，任何无意义的旋转都不改变 covariance，因此也不存在 eigenvector label ambiguity。

理论上，任意两个具有相同 spectrum 的 SPD 矩阵均可通过 orthogonal conjugation 联系；考虑 eigenvector 符号自由度后可取 (Q\in SO(6))，而任意 (SO(6)) 元素都有 skew-symmetric logarithm。因此该层对 isospectral orientation correction 是完整的，而非局部 heuristic。

---

## 9.3 群兼容的 matrix temperature scaling

K-only 后再增加低容量 spectral calibrator。不要使用固定 Kelvin basis 下的任意

[
S\mapsto ASA^\top,
]

因为固定 (A) 要保持 equivariance，必须满足

[
A\rho(g)=\rho(g)A.
]

对 multiplicity-free 的 (V=0e\oplus2e)，这只允许

[
A=aP_{0e}+bP_{2e},
]

无法进行一般 orientation correction。

更合适的是在 log-SPD 空间做 commutant calibration：

[
L_{\rm ori}=\log S_{\rm ori},
]

[
L_{\rm cal}
===========

\mathcal T_\psi(L_{\rm ori})+b,
\qquad
S_{\rm cal}=\exp L_{\rm cal}.
]

因为

[
\operatorname{Sym}^2(V)
=======================

2\cdot0e\oplus2\cdot2e\oplus4e,
]

所有线性 O(3)-equivariant (\mathcal T) 的一般形式为

[
\mathcal T
==========

(M_0\otimes I_1)
\oplus
(M_2\otimes I_5)
\oplus
(t_4 I_9),
]

其中

[
M_0,M_2\in\mathbb R^{2\times2},
\qquad
t_4\in\mathbb R,
]

而 bias (b) 只能位于两个 (0e) 通道。总参数量约为 11，可在 identity 附近正则化。这是严格群论约束下的“matrix temperature scaling”，比 scalar temperature 更有表达力，又远小于重新训练一个 full covariance network。

推荐主实验层级：

1. scalar temperature；
2. irrep temperature (aP_0+bP_2)；
3. commutant log-temperature；
4. K-only orientation；
5. K + scalar；
6. K + commutant log-temperature。

---

## 9.4 训练协议

推荐四阶段训练。

### 阶段 A：mean-only

以 point accuracy 为目标训练 (\mu_\theta)。若 physical MAE 在 physical dielectric space 计算，而训练输出位于 log-KM space，应明确二者不等价：

[
\exp(\mathbb E[\log\varepsilon])
\neq
\mathbb E[\varepsilon].
]

可同时报告 log-KM loss 和 physical-space MAE，必要时加入小权重的 Monte Carlo/delta-method physical loss。此项是 dielectric-specific。

### 阶段 B：基础 covariance

冻结 mean，使用 cross-fitted residual 训练 (S_\theta)。covariance gradient 不回传 mean head，最好也不回传主要 backbone，或只经过独立 equivariant covariance adapter。

主损失：

[
\mathcal L_{\rm base}
=====================

\operatorname{NLL}*t
+
\lambda*{\rm ES}ES
+
\lambda_{\rm reg}R_{\rm spectrum}.
]

使用 centered spectral window，将 log-volume 和 condition control 分离。

### 阶段 C：orientation calibration

冻结 mean、base covariance eigenvalues 和 (\nu)，在独立 calibration-A 集上训练 (K_\phi)：

[
\mathcal L_{\rm ori}
====================

\operatorname{NLL}*t(S*{\rm ori})
+
\lambda_{\rm slice}S_{\rm slice}
+
\lambda_K|K_\phi|_F^2.
]

(K_\phi) 应零初始化。建议只使用 1–2 层 CG head，避免 calibration set 过拟合。

### 阶段 D：spectral 与 conformal

在 calibration-A 上拟合 scalar 或 commutant log-temperature。在完全独立的 calibration-B 上计算 Mahalanobis conformal quantile，最终给出 coverage guarantee。

---

## 10. 工程实现建议

### e3nn

用 compiler 同时生成：

[
\operatorname{Sym}^2(V)
\quad\text{和}\quad
\Lambda^2(V).
]

为 symmetric 与 skew matrix 分别预计算 Frobenius-orthonormal CG basis：

[
H=\sum_a h_aB_a^{\rm sym},
\qquad
K=\sum_b k_bB_b^{\rm skew}.
]

需要测试：

[
B_a:B_b=\delta_{ab},
]

[
B(D(g)c)
========

\rho(g)B(c)\rho(g)^\top.
]

使用 e3nn 的 equivariance testing 对 mean、symmetric lift、skew lift、matrix exponential 和完整 Student-t log-prob 分别测试，而不只测试最终输出。e3nn 官方提供 `assert_equivariant`，其 `Irreps` 明确编码 O(3) rotation/inversion transformation。([E3NN][12])

### cuEquivariance

先在 e3nn 中建立 reference implementation，再替换 tensor-product kernels。cuEquivariance 提供 segmented tensor-product 描述、CUDA kernels 以及 PyTorch/JAX bindings，适合优化 CG-heavy covariance/orientation heads。需要固定并验证：

* irrep ordering；
* multiplicity layout；
* CG normalization；
* parity convention；
* e3nn 与 cuEquivariance 的 channel layout。

不能只比较输出 norm；应逐 irrep 比较 reference coefficients。([NVIDIA Docs][13])

### PyTorch / Pyro

Student-t NLL 最稳妥的实现路径是：

1. 构造 symmetric (S)；
2. `torch.linalg.cholesky_ex`；
3. triangular solve 计算 (\delta)；
4. Cholesky diagonal 计算 logdet；
5. 禁止显式 `inverse()`。

Pyro 已有 `MultivariateStudentT(df, loc, scale_tril)`，可作为 log-prob 和 sampling 的数值 reference。([Pyro 文档][1])

建议所有 SPD/equivariance unit tests 使用 float64，并在训练中记录：

* Cholesky failure count；
* min/max log-eigenvalue；
* condition quantiles，而非只记录 mean condition；
* saturation at spectral-window boundaries；
* (K) 的 Frobenius norm；
* equivariance error 随 condition number 的关系。

### Geoopt / Geomstats

适用于：

* 学习少量全局 SPD calibration 参数；
* affine-invariant、log-Euclidean 或 Bures 型 regularizer；
* calibration matrices 的 geodesic interpolation；
* SPD geometry sanity checks。

不建议把每样本 covariance head 强行改造成 manifold parameter，因为 symmetric matrix exponential 已经是自然的 Euclidean-to-SPD chart。

### Calibration libraries

`netcal`、Uncertainty Toolbox 一类库适合 reliability diagram、quantile calibration 和常规 regression metrics，但其标准接口主要面向 scalar 或 coordinate-wise uncertainty，并不理解 (O(3)) representation、Student-t scale convention 或 6D covariance conjugation。应只复用 plotting/metric infrastructure，核心 Mahalanobis、irrep whitening 和 group-averaged scoring 自行实现。([PyPI][14])

---

## 11. TPAMI 实验设计

建议论文中至少包含以下证据链。

### Controlled synthetic experiment

人工生成正确 eigenvalues，但施加已知 equivariant orientation corruption：

[
S_{\rm corrupt}(x)
==================

Q_{\rm true}(x)^\top
S_{\rm true}(x)
Q_{\rm true}(x).
]

验证 K-head 能恢复 orientation，而 scalar、irrep temperature 和普通 variance scaling 不能。该实验可隔离 expressivity、calibration 和 conditioning。

### Dielectric experiment

报告：

[
\text{physical MAE},\quad
\text{Student-t NLL},\quad
ES,\quad
\text{sliced CRPS},
]

[
\operatorname{coverage}@{50,80,90,95},
]

[
\mathbb E[\delta],\quad
\operatorname{QuantileError}(\delta),
]

[
A_{\rm white},
\quad
|\Pi_{0e^\perp}D|,
\quad
|\Pi_{2e}^{(1)}D|,
\quad
|\Pi_{2e}^{(2)}D|,
\quad
|\Pi_{4e}D|,
]

以及 logdet、condition quantiles、equivariance error。

### 因果性消融

最关键的对照是：

* base；
* scalar temperature；
* K-only；
* spectrum-only；
* K+spectrum；
* K+spectrum+conformal。

K-only 保持 spectrum、determinant 和 condition 完全不变，因此它提供了非常干净的 orientation causal ablation。

### 泛化任务

若论文声称是 probabilistic structured prediction framework，而不是 dielectric calibration paper，至少增加：

* (V=1o) 或 (3\times1o) 的 vector/force uncertainty；
* 一个不同 mixed-irrep output；
* synthetic arbitrary representation benchmark。

只在 dielectric 上展示 (1e\oplus2e\oplus3e) skew calibrator，容易被认为是 task-specific engineering；在多个 (V) 上自动编译 (\Lambda^2(V)) 才能支持“representation-generic”的 TPAMI 主张。

---

## 12. 普适方法与 dielectric-specific heuristic

### 普适、可作为框架贡献

* 从 (V) 自动编译 (\operatorname{Sym}^2(V)) covariance representation。
* 从 (V) 自动编译 (\Lambda^2(V)) orientation-calibration representation。
* (S\mapsto e^KSe^{-K}) 的 equivariant isospectral calibration。
* commutant-constrained log-matrix temperature scaling。
* log-volume 与 centered log-shape 分离的 spectral window。
* cross-fitted residual 与 mean/covariance gradient decoupling。
* radial PIT + angular whitening + irrep-resolved calibration diagnostics。
* NLL、O(d)-invariant energy/sliced CRPS 的 proper-score 组合。
* orientation/spectral calibration 后的 Mahalanobis split conformal。
* equivariant low-rank-plus-irrep-isotropic covariance。

### 仅对 dielectric 特别自然

* (V=0e\oplus2e) 的具体分解：
  [
  \operatorname{Sym}^2(V)=2\cdot0e\oplus2\cdot2e\oplus4e,
  ]
  [
  \Lambda^2(V)=1e\oplus2e\oplus3e.
  ]
* 对 trace-like dielectric channel 与 deviatoric channel 使用独立温度。
* 按 crystal system、space group 或材料类别分层诊断 calibration。
* 在 physical dielectric tensor 经 matrix exponential 后计算 MAE。
* 利用 dielectric positivity 或材料先验设置 log-spectrum 范围。

### 不建议作为核心方法的 dielectric heuristic

把 covariance eigenvectors 强制对齐到 predicted dielectric tensor 的 principal axes，可能在某些晶体数据上有效，但它：

* 假设误差方向由目标张量自身的 eigenframe 决定；
* 在重复 eigenvalue 附近不连续；
* 不能表示与局域化学环境相关、但不与 dielectric eigenframe 共线的 uncertainty；
* 很难推广到一般 (V)。

它适合作为 ablation 或先验 baseline，不适合作为 TPAMI 方法的理论核心。

---

## 最终推荐

最适合当前问题和 TPAMI 叙事的方案是：

[
\boxed{
S_{\rm base}
;\xrightarrow[\Lambda^2(V)]{\text{equivariant isospectral }e^K}
S_{\rm ori}
;\xrightarrow[\operatorname{Sym}^2(V)]{\text{commutant log-temperature}}
S_{\rm cal}
;\xrightarrow{\text{split conformal}}
\mathcal C_\alpha
}
]

配套使用：

[
\boxed{
\text{frozen/cross-fitted mean}
+
\text{Student-t NLL}
+
\text{isotropic sliced CRPS}
+
\text{irrep-resolved whitened diagnostics}
}
]

其中最有论文价值的贡献不是“再增加一个 covariance head”，而是把误校准明确分解成：

[
\text{location error}
\oplus
\text{radial/volume error}
\oplus
\text{spectral-shape error}
\oplus
\text{isospectral-orientation error},
]

并为 orientation 部分给出由 (\Lambda^2(V)) 自动编译、严格保持 (O(3)) equivariance、保持 SPD/spectrum/condition number 的校准层。这与现有 scalar temperature、固定 basis matrix scaling 和普通 conformal ellipsoid 有清晰的理论差异。


[1]: https://docs.pyro.ai/en/stable/_modules/pyro/distributions/multivariate_studentt.html "https://docs.pyro.ai/en/stable/_modules/pyro/distributions/multivariate_studentt.html"
[2]: https://arxiv.org/html/2510.21691v1 "https://arxiv.org/html/2510.21691v1"
[3]: https://docs.e3nn.org/en/stable/api/o3/o3_tp.html "https://docs.e3nn.org/en/stable/api/o3/o3_tp.html"
[4]: https://proceedings.mlr.press/v206/stirn23a/stirn23a.pdf "https://proceedings.mlr.press/v206/stirn23a/stirn23a.pdf"
[5]: https://geoopt.readthedocs.io/en/latest/_modules/geoopt/manifolds/symmetric_positive_definite.html?utm_source=chatgpt.com "Source code for geoopt.manifolds.symmetric_positive_definite"
[6]: https://proceedings.iclr.cc/paper_files/paper/2025/file/e962ee491a659f957f0d435f7a133e49-Paper-Conference.pdf "https://proceedings.iclr.cc/paper_files/paper/2025/file/e962ee491a659f957f0d435f7a133e49-Paper-Conference.pdf"
[7]: https://sites.stat.washington.edu/raftery/Research/PDF/Gneiting2007jasa.pdf?utm_source=chatgpt.com "Strictly Proper Scoring Rules, Prediction, and Estimation"
[8]: https://arxiv.org/abs/1910.07325?utm_source=chatgpt.com "Multivariate Forecasting Evaluation: On Sensitive and Strictly Proper Scoring Rules"
[9]: https://journals.ametsoc.org/view/journals/mwre/143/4/mwr-d-14-00269.1.xml?utm_source=chatgpt.com "Variogram-Based Proper Scoring Rules for Probabilistic ..."
[10]: https://arxiv.org/abs/2409.14456?utm_source=chatgpt.com "Scoring rule nets: beyond mean target prediction in multivariate regression"
[11]: https://arxiv.org/abs/2105.14957?utm_source=chatgpt.com "Conformal Uncertainty Sets for Robust Optimization"
[12]: https://docs.e3nn.org/en/latest/guide/equivar_testing.html "https://docs.e3nn.org/en/latest/guide/equivar_testing.html"
[13]: https://docs.nvidia.com/cuda/cuequivariance/ "https://docs.nvidia.com/cuda/cuequivariance/"
[14]: https://pypi.org/project/netcal/?utm_source=chatgpt.com "netcal"
